## Packages
recharts | For rendering beautiful charts in the Admin Dashboard
framer-motion | For smooth page transitions and component animations
lucide-react | Standard icons (already in stack, but explicitly needed for UI)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Roles are "student", "mentor", "admin"
- /api/student/resume expects FormData (multipart/form-data)
- Assuming all other endpoints accept application/json
